import { Typography } from '@material-ui/core'
import React from 'react'

const Redirect = () => {
    return (
        <div>
            <Typography variant="h5">Sign up or Login now to start tracking your expenses with PIGGY BANK</Typography>
        </div>
    )
}

export default Redirect
